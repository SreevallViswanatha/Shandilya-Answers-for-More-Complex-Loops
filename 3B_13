// WAP to convert Hexadecimal to Binary number.
#include <stdio.h>
#include <stdlib.h>
#include<string.h>
main()
{
    char hexadecimal[1000], binary[1000]="", remainder;
    int length;
    printf("Enter a hexadecimal number:");
    scanf("%s", hexadecimal);
    length=strlen(hexadecimal)-1;
    for(; length>=0; length--)
    {
        remainder=hexadecimal[length-1];
        switch(remainder)
        {
        case '0':

        }
    }
}
